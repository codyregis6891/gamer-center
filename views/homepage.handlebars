<div class="row justify-content-md-center" id="homebody">
    <div class="col col-lg-10">
        {{#if logged_in}}
        <h1>Hello {{username}}!</h1>
        <h2>Welcome Back</h2>
        {{else}}
        <h1>Welcome Guest!</h1>
        <h2>Please Log In or Sign Up</h2>
        {{/if}}
    </div>
</div>
